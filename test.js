const assert = require('assert')
const request = require('supertest')

describe('developers API should have endpoints to', () => {
    it('get all developers', () => {
        // arrange

        // act

        // assert
        assert.strictEqual(false, true)
    })
})